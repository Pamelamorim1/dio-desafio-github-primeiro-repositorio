// @Autora: Pâmela Taiane

const funcoes = require('./funcoes-auxiliares');

console.log(funcoes);

console.log(funcoes.get());

const pessoa = {
    nome: 'Victor'
}

const { nome } = pessoa;

console.log(nome)